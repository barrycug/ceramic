<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Metatile for <%= params[:z] %>/<%= params[:x] %>/<%= params[:y] %></title>
    <style>
      
      body {
        background: #eee;
      }
      
      canvas {
        background: #fff;
        position: absolute;
      }
      
    </style>
  </head>
  <body>
    
    <script src="/debug_render.js"></script>
    <script>
    
      var metatile = {
        z: <%= params[:z] %>,
        x: <%= params[:x] %>,
        y: <%= params[:y] %>,
        size: <%= params[:size] %>
      };
    
      var tiles = [<%= @tiles.join(",\n") %>];
      var i;
      
      for (i = 0; i < tiles.length; i++) {
      
        canvas = document.createElement("canvas");
        
        canvas.title = metatile.z + "/" + (metatile.x + Math.floor(i / metatile.size)) + "/" + (metatile.y + (i % metatile.size));
        
        canvas.width = 148;
        canvas.height = 148;
        
        canvas.style.left = (Math.floor(i / metatile.size) * 150) + "px";
        canvas.style.top = ((i % metatile.size) * 150) + "px";
        
        context = canvas.getContext("2d");
      
        DebugRender.renderTile(canvas, tiles[i], 10);
        
        document.body.appendChild(canvas);
        
      }
      
    </script>
    
  </body>
</html>
